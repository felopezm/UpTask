extends layout

include mixins/addProject
    


block contenido
    .contenido-principal
        h1 #{namePag}

        if errores
            each error in errores
                .alerta= error.texto
        +addProject(project)

        
